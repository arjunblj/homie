# homie

Give your AI a personality, not a prompt.

This repo is a Bun workspace monorepo:

- `packages/homie-ai`: runtime + `homie` CLI (npm package: `homie-ai`)
- `packages/create-homie`: interactive wizard (npm package: `create-homie`)

