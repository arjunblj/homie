{
  "name": "create-homie",
  "version": "0.0.0",
  "description": "Interactive wizard to create a homie friend project.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "create-homie": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json && bun build ./src/index.ts --outdir dist --target bun --format esm --sourcemap",
    "typecheck": "tsc -p tsconfig.json",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.3.0",
    "node": ">=18"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.44",
    "@ai-sdk/openai-compatible": "^2.0.30",
    "@clack/prompts": "^1.0.1",
    "ai": "^6.0.86",
    "zod": "^4.3.6"
  }
}
